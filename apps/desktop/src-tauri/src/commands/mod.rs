//! Tauri command adapters.
//!
//! This module is the target home for transport-level command handlers. Existing
//! commands are still exposed from legacy modules while the backend is migrated
//! into command, application, domain, and infrastructure layers.
pub mod diagnostics;
