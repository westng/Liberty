# Liberty 项目 Agent 规则

## 适用范围

- 本文件位于仓库根目录，适用于整个 Liberty 仓库。更深目录中的 `AGENTS.md` 可为其目录树补充或覆盖规则。
- Liberty 是本地优先的桌面会议音视频处理应用，不是单纯的 Web 项目。主技术栈为 React 19、TypeScript、Vite、Tauri 2、Rust、SQLite，以及托管 Python 3.10/FunASR/ffmpeg 运行时。
- 修改前先阅读与任务直接相关的当前源码、测试和文档。`docs/ai/` 保存过程文档与迁移后的历史快照；若与当前代码、`README.zh-CN.md` 或 `docs/architecture/` 冲突，以当前实现和当前架构文档为准，并明确指出差异。

## 分析与判断

- 回答或执行前，先检查问题中的关键前提是否成立、推理是否存在逻辑跳跃、完成判断还缺少哪些信息；无法核实的前提要明确标注，并说明它会如何影响结论，不能把用户的表述直接当成已证实事实。
- 保持独立判断，不以附和用户、维持语气一致或满足预设结论代替分析；用户的目标、偏好和假设都可以被检验，但不要为了反驳而反驳。
- 当事实、推测和主观观点混合出现且会影响决策时，明确区分：事实应可由代码、日志、测试、文档或可靠来源支持；推测应说明依据、关键假设和不确定性；主观观点或建议应说明采用的评价标准与取舍。
- 涉及数字、人物、时间、归因、引用或重要结论时，尽量核实原始来源或权威来源，并关注来源日期、统计口径、适用范围和版本；仓库内事实优先以当前代码、配置、测试和运行结果为准。无法核实时，不编造出处或确定性结论，应明确写出“未核实”及其原因。
- 如果证据不支持用户的判断，应直接说明不同意或证据不足，并同时给出依据、可能风险、至少一种合理的替代解释，以及验证各解释所需的下一步证据。
- 主动提醒可能被忽略但足以改变结论的变量，包括时间范围、样本与幸存者偏差、基准和对照、依赖条件、边界情况、实施与维护成本、机会成本、兼容性、安全与合规、迁移和回滚成本；优先提示高影响项，不机械罗列与任务无关的风险。
- 证据相互冲突时，不强行合并为单一结论；说明冲突点、各来源可信度、当前更倾向的解释和仍需验证的内容。对时效性强的信息注明核实时间，避免把历史事实当成当前状态。
- 简单、低风险且可直接验证的任务无需套用固定分析模板；高风险、不可逆或会影响业务决策的任务，应提高核实强度，并清楚说明结论的置信度、适用条件和未覆盖范围。

## 工作边界

- 只修改完成当前任务所需的文件；保留用户已有的未提交改动，不顺手重构或修复无关问题。
- 诊断、实现、构建、提交、推送、创建标签、发布 Release 和部署是不同操作。未得到相应授权时，不扩大到后续操作。
- 不手工修改 `apps/desktop/src-tauri/gen/` 或 `apps/desktop/src-tauri/permissions/autogenerated/` 下的生成文件。应修改命令注册、capability 或生成源，再通过项目工具生成产物。
- 不提交构建缓存、运行时下载物、本地数据库、凭据、日志或打包输出。遵守 `.gitignore`，尤其是 `target/`、`dist/`、`runtime-bundles/`、`.liberty-runtime/`、`.liberty-secrets/` 和 release 输出目录。
- 不在日志、诊断、测试夹具、截图或提交内容中写入 API Key、Token、兑换签名密钥、用户媒体路径或其他敏感信息。

## 项目结构与架构

- `apps/desktop/src/app/` 负责应用壳、路由、启动和全局装配。
- `apps/desktop/src/features/` 按用户功能组织。View 负责布局、交互和 UI 状态协调；轮询、合并策略和长流程编排应进入对应的 `application/` 或 store/use case，不堆入页面组件。
- `apps/desktop/src/shared/` 只放跨功能复用的组件、类型、i18n、服务和样式。与单一业务域绑定的逻辑留在对应 feature。
- 前端调用本地能力时使用 `shared/services/tauri/` 的类型化适配；远端会议模式通过 `shared/services/remote/meetingApi.ts`，不要在 View 中散落 `invoke` 或 HTTP 请求。
- Rust 的 Tauri command 是传输适配层：负责输入校验、窗口/权限边界和调用应用服务，不直接承载 SQL、子进程编排或文件系统策略。
- 业务实体、状态机、策略和类型化错误放在 `domain/`；用例编排放在 `application/`；SQLite repository、运行时、进程、凭据、文件系统和平台实现放在 `infrastructure/`。
- 平台相关能力使用清晰的 `cfg` 边界，并同时考虑 macOS Apple Silicon、macOS Intel、Windows x64 和 Windows x86；不得用当前开发机运行成功替代跨平台兼容性判断。

## 前端规则

- TypeScript 保持 `strict`、`noUnusedLocals`、`noUnusedParameters` 和 `noFallthroughCasesInSwitch` 通过；不以 `any`、忽略指令或无意义类型断言绕过真实错误。
- 沿用 React 函数组件和现有状态组织方式。优先复用已有组件、服务、i18n 和样式分区，不引入第二套路由、状态管理或样式体系。
- 用户可见文案应同时考虑 `zh-CN` 与 `en-US`。优先维护 `shared/i18n/messages/` 的类型化消息树；若所在模块沿用局部双语映射，也必须保持两种语言一致。
- 保持无障碍与原生桌面交互语义：表单控件应有标签，图标按钮应有可读名称，弹窗应处理标题、焦点和关闭路径，不能只依赖颜色表达状态。
- 修改共享类型或前后端 DTO 时，追踪所有本地 Tauri、远端 API、持久化快照和 UI 消费方，避免只修一端。

## Rust、SQLite 与运行时规则

- Rust 代码必须通过 `cargo fmt` 与 Clippy 的 warnings-as-errors；新增行为应在相邻模块补充单元测试，特别是状态转换、迁移、路径处理、协议解析和平台分支。
- SQLite schema 变更必须加入 `apps/desktop/src-tauri/src/local_db/schema.rs` 的顺序化、版本化迁移，并同步 `CURRENT_SCHEMA_VERSION` 约束；不得在普通业务路径执行临时 `ALTER TABLE`。
- 迁移应支持旧库升级、重复启动不重复应用，并在涉及数据或凭据变换时覆盖失败与重试。不要删除、覆盖或重建用户的本地数据库来规避迁移问题。
- SQL 归对应的 `infrastructure/repositories/`；应用服务调用类型化 repository 操作，不在 command 或前端拼接 SQL。
- 长任务以持久化 job snapshot 和结构化 `job_events` 为事实来源；文本日志只是面向用户的投影，不能成为唯一状态依据。
- Python Runner 的 stdout 协议是 JSON Lines。协议字段变化要同步 Rust 解析、错误处理、测试和相关文档，并确保普通日志不会污染协议输出。
- `apps/desktop/src-tauri/resources/runtime-manifest.json` 是 Python、ffmpeg、模型、下载源和平台支持的事实来源。资源必须固定版本并提供真实 URL 与 SHA-256；不得使用 `latest`、占位地址或未校验资产冒充可用来源。
- Python 依赖锁文件按目标平台维护，保持哈希锁定；修改 `requirements.txt`、Runner 或运行时版本时，同步对应 lock、warmup/validate 逻辑和运行时检查。

## Tauri、安全与权限

- 新增、删除或重命名 Tauri command 时，至少同步检查：Rust command 实现、`src/lib.rs` 的 `generate_handler!`、`src-tauri/build.rs` 的命令清单、目标窗口的 `capabilities/*.json`，以及前端 `shared/services/tauri/` 调用适配。
- capability 按窗口最小授权。主窗口可承担编排、对话框、导出和子窗口创建；结果窗口与编辑窗口只获得完成自身职责所需的权限，不因开发方便扩大权限。
- 文件系统权限必须使用明确 scope；外部 URL、子进程、归档解压和用户选择路径都要校验边界，防止路径穿越、命令注入或任意文件访问。
- API Key 与远端 Token 存入系统凭据存储（macOS Keychain / Windows Credential Manager），SQLite 只保存引用；不得新增明文凭据回退。
- 生产 CSP、安全基线和诊断脱敏不能因调试而关闭。确需临时诊断时，使用局部、可撤销且不进入发布配置的手段。
- 宠物奖励、LP、盲盒和兑换 Key 是本地玩法，不得在没有明确产品与合规设计的情况下引入真钱、充值、交易、付费概率或排行榜语义。

## 文档治理

- Liberty 的 AI 过程文档入口是 `docs/ai/README.md`。所有新增过程文档先遵循该入口定义的分类、元数据和权威边界；不得把其他项目的名称、路径、环境结论或治理规则直接复制进来。
- `docs/ai/designs/` 放设计方案，`docs/ai/plans/` 放实施计划，`docs/ai/research/` 放调研与诊断，`docs/ai/reviews/` 放评审与审计快照，`docs/ai/handoffs/` 放交接记录；目录按需创建，不为占位批量建立空目录。
- 原 Superpowers 过程文档已迁入 `docs/ai/designs/` 和 `docs/ai/plans/`，旧插件及 `docs/superpowers/` 路径均已弃用，不得恢复或新增引用。维护迁移文档时应保留历史上下文，并明确其状态和适用版本。
- 正式文档继续留在稳定路径：项目入口放根 README，架构与发布规则放 `docs/architecture/`，当前业务规则放 `docs/` 下对应主题文件，模块说明放模块自己的 README；需要提供可审核 SQL 时可按需创建 `docs/sql/`。当前仓库没有 `docs-site/`，不得引用不存在的文档站结构。
- 是否进入 `docs/ai/` 取决于文档职责，不取决于是否使用过 AI 辅助措辞。面向用户、开发者或运维持续维护的正式文档，不得仅因 AI 参与编写而迁入过程文档区。
- AI 过程文档必须声明文档类型、状态、创建或核实日期、适用范围、权威边界和依据。文档中的状态、路径、行号、测试结果、环境事实与完成度在引用前必须根据当前仓库或当前环境重新核对。
- 已确认且需要长期维护的结论必须同步提炼到代码、测试、契约或正式文档。过程文档可以保留、归档或删除，但不得成为当前系统行为、接口、数据规则或发布要求的唯一依据。
- 不提交聊天全文、提示词、隐藏思考过程、临时日志、秘密、机器或地区专属配置、未经脱敏的用户媒体信息或生产数据。诊断样本只保留复现所需的最小脱敏内容。

## 版本、锁文件与发布

- 使用仓库固定的 `pnpm@10.30.3`、Rust `1.94.0` 和现有 lockfile；不无故升级工具链或依赖。安装依赖使用 `pnpm install --frozen-lockfile`，只有依赖声明确实变化时才更新锁文件。
- 发布版本必须在根 `package.json`、`apps/desktop/package.json`、`apps/desktop/src-tauri/tauri.conf.json`、`apps/desktop/src-tauri/Cargo.toml` 和 `Cargo.lock` 中一致，并为 `CHANGELOG.md` 添加对应版本的中英文记录。
- 发行标签严格使用 `v<semver>`。macOS Apple Silicon/Intel 与 Windows x64/x86 资产必须按平台区分，不能共用未经验证的运行时假设。
- 不手工削弱 `.github/workflows/`、`scripts/check-release-*.mjs` 或安全检查来让发布通过。发布修复应保留 quality gate、最小 GitHub 权限、固定 Action SHA 和资产校验。
- 构建成功不代表发布完成。获授权发布后，还要分别验证目标提交、标签指向、Release 状态、Release ID 绑定、预期资产名称/数量和校验结果，并明确报告每一项结果。

## 验证与完成标准

- 从最小相关检查开始，再按风险扩大：
  - 仅前端 TS/TSX：`pnpm desktop:typecheck`；涉及打包入口、CSS 或资源解析时再运行 `pnpm desktop:build:web`。
  - Rust：相邻测试或目标 package 测试，然后 `pnpm rust:fmt:check`；影响公共 Rust 路径时再运行 workspace tests 与 Clippy。
  - 运行时/平台/安全/版本：运行对应的 `pnpm release:check-*`；涉及发布流程时再运行 policy 与自测脚本。
  - 跨层、数据库、Tauri command、权限或发布相关变更：完成前运行与 CI 等价的 `pnpm check`，并补充任务特定验证。
- 不把“命令退出 0”“进程存在”“文件非空”单独当成功证据；应验证用户可观察结果、持久化结果或目标产物。
- 用户要求“启动客户端”时，使用 `pnpm desktop:tauri dev` 启动开发版原生客户端。Vite 地址只是内部依赖，不能作为客户端已启动的证据；还应确认当前 `target/debug/liberty` 进程和原生窗口实际出现，不要打开可能陈旧的 `target/debug/bundle/macos/Liberty.app`。
- 避免同时启动多个 Liberty 实例；它们会竞争同一个本地 SQLite 数据库。运行中的只读锁不等于数据丢失，不能据此删除或重置数据库。
- 无法在当前平台验证的目标（尤其 Windows x86/x64、签名、公证和安装器）必须明确标为“未验证”，说明已完成的替代检查及剩余风险。
- 最终说明应列出修改范围、执行过的验证及结果、未验证项和阻塞；不要声称未实际执行的构建、原生交互、跨平台或发布验证已经完成。
